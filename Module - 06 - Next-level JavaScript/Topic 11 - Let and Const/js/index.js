// let block definitions
// with const we can add values on any array or objects, but we cannot assign a new value on them

const example = {};
example.name = "David"
example.surname = "Macias"
console.log(example)